<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp">

    <RadioGroup
        android:id="@+id/rg_backends"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <RadioButton
            android:id="@+id/rb_ohb"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="8dp"
            android:text="@string/server_ohb_label"
            android:textColor="@color/hamclock_text"
            android:textSize="16sp" />

        <RadioButton
            android:id="@+id/rb_hamclock"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="8dp"
            android:text="@string/server_hc_label"
            android:textColor="@color/hamclock_text"
            android:textSize="16sp" />

        <RadioButton
            android:id="@+id/rb_custom"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="8dp"
            android:text="@string/server_custom_label"
            android:textColor="@color/hamclock_text"
            android:textSize="16sp" />
    </RadioGroup>

    <EditText
        android:id="@+id/et_custom_backend"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="32dp"
        android:layout_marginTop="4dp"
        android:hint="@string/custom_server_hint"
        android:inputType="textUri"
        android:singleLine="true"
        android:textColor="@color/hamclock_text"
        android:textColorHint="#888888"
        android:visibility="gone" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        android:background="#33FFFFFF" />

    <Button
        android:id="@+id/btn_open_setup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/open_setup"
        android:textColor="@color/hamclock_text"
        android:backgroundTint="#2C3E50" />

</LinearLayout>
